# 《SWE-agent: Agent-Computer Interfaces Enable Automated Software Engineering》解读

| 项目 | 内容 |
|---|---|
| 作者/机构 | John Yang 等，Princeton |
| 发表 | arXiv:2405.15793（NeurIPS 2024） |
| 一句话总结 | 提出 **ACI（Agent-Computer Interface）** 概念：为 agent 重新设计的文件编辑/命令接口让 5 亿参数级模型也能修真实 GitHub bug——harness 工程的奠基论文 |

## 1. 研究背景与问题

- GPT-4 直接驱动 shell 修 bug 的效果很差——因为 shell/编辑器是**为人类**设计的（输出噪声大、编辑不精确）。
- 问题：**如果接口为 agent 重新设计，能力上限能提高多少？**

## 2. 核心方法（ACI 设计原则）

1. **简化观察**：目录列表带文件长度、搜索结果显示行号上下文——减少 agent 的读取次数。
2. **防蠢保护**：编辑命令自动 lint，失败即拒绝并回显错误。
3. **专用编辑原语**：`edit`（精确替换）、`open`（带行号）、`scroll`——比"sed/awk 拼命令"稳定一个数量级。
4. **LM 友好的错误处理**：把 traceback 截断、突出关键行。

## 3. 关键实验与结果

- 在 SWE-bench 上达到当时 SOTA（12.29%），且消融证明：**接口设计的贡献 > 换更强的模型**。

## 4. 局限性与争议

- ACI 需要针对每个环境重新设计（浏览器/IDE/数据库各一套）；界面越专用、泛化越难。

## 5. 与本知识库的关联

模块 05 第 0 节"harness 收益大于模型"的实证出处；设计任何 agent 工具接口时的检查清单。

## 6. 阅读建议

- 精读 §2（ACI 设计三原则）与 §3.3（编辑命令的消融）。对照 Claude Code 的 Edit/Read 工具设计，体会同一思想的工业化版本。
